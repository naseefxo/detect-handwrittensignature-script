This project uses a custom YOLOv8 model to detect handwritten signatures in scanned images or documents.

Install required packages with:


pip install opencv-python ultralytics supervision huggingface_hub
